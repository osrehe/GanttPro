# Copia este archivo a .env y ajusta los valores.

# Puerto del host que publica Postgres (docker compose lo lee de este archivo).
# Cámbialo si el 5432 ya está ocupado en tu máquina y ajusta las URLs de abajo.
POSTGRES_PORT=5432

# Base de datos (Postgres 16 levantado con `docker compose up -d`)
DATABASE_URL="postgresql://ganttpro:ganttpro@localhost:5432/ganttpro?schema=public"
# Base de datos usada por los tests de integración (creada por docker/postgres/init)
DATABASE_URL_TEST="postgresql://ganttpro:ganttpro@localhost:5432/ganttpro_test?schema=public"

# Auth.js: secreto para firmar la sesión. Genera uno con `npx auth secret` u `openssl rand -base64 32`.
AUTH_SECRET="cambia-este-secreto-por-uno-largo-y-aleatorio"
# Necesario fuera de Vercel para aceptar el host de la petición.
AUTH_TRUST_HOST=true

# Contraseña de los usuarios de ejemplo que crea `npm run db:seed`
# (admin@ganttpro.local, editor@ganttpro.local, lector@ganttpro.local).
SEED_PASSWORD="GanttPro2026!"

# Exportación PDF (Paso 9). Opcionales.
# URL con la que el propio servidor abre /print/gantt en Chromium (por defecto, el origen de la petición).
# Ruta a un Chromium/Chrome ya instalado (por defecto usa el que descarga Puppeteer).
# PUPPETEER_EXECUTABLE_PATH="C:/Program Files/Google/Chrome/Application/chrome.exe"

# URL con la que el servidor se llama a sí mismo para imprimir el PDF (Puppeteer).
# Solo es necesaria si el proceso no escucha en 127.0.0.1:PORT (por ejemplo, detrás de un proxy
# dentro de otro contenedor). Nunca se deduce de las cabeceras de la petición.
# PRINT_BASE_URL="http://127.0.0.1:3000"

# Inicio de sesión con Google (Paso 10). Opcional: si faltan, solo se ofrece correo y contraseña.
# El correo de la cuenta de Google debe existir ya en la tabla User; no se crean cuentas solas.
# AUTH_GOOGLE_ID="xxxxxxxx.apps.googleusercontent.com"
# AUTH_GOOGLE_SECRET="tu-secreto-de-google"
# Muestra el botón "Entrar con Google" en /login (debe coincidir con las variables anteriores).
# NEXT_PUBLIC_AUTH_GOOGLE=1

# Notificaciones de menciones en comentarios (Paso 10). Por defecto se escriben en la consola del
# servidor; SMTP queda preparado para producción y todavía no envía nada.
# MAIL_TRANSPORT="console"
# MAIL_SMTP_HOST="smtp.tuproveedor.cl"
# MAIL_SMTP_PORT="587"
# MAIL_SMTP_USER="notificaciones@tudominio.cl"
# MAIL_SMTP_PASSWORD="tu-clave-smtp"
# MAIL_FROM="GanttPro <notificaciones@tudominio.cl>"

# Límite de peticiones (Paso 11, ADR-012). Solo aprietan en producción: fuera de producción el
# valor por defecto es muy alto para no estorbar al desarrollo ni a la suite de pruebas.
# RATE_LIMIT_LOGIN_MAX=10   # intentos de inicio de sesión por IP cada 5 minutos
# RATE_LIMIT_API_MAX=120    # escrituras de la API por IP cada minuto

# Despliegue de producción con docker-compose.prod.yml. `DATABASE_URL` apunta al servicio `db`
# de la propia red de Docker y `PUPPETEER_EXECUTABLE_PATH` al Chromium que trae la imagen.
# POSTGRES_PASSWORD="una-clave-larga-y-distinta-de-la-de-desarrollo"
# APP_PORT=3000
# DATABASE_URL="postgresql://ganttpro:una-clave-larga@db:5432/ganttpro?schema=public"
# PUPPETEER_EXECUTABLE_PATH="/usr/bin/chromium"
